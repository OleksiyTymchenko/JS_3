/*=================== 1 task =================*/

//const aNswer;

const perfectNumber = (nUmber) => {
  a = (nUmber * (nUmber + 1)) / 2;
  return alert(a);
};

const nUmber = +prompt(`Enter your number`);

perfectNumber(nUmber);
